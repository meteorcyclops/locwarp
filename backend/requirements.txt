# v0.2.196's userspace WiFi tunnel API (UserspaceRsdTunnel/UserspaceDialPlane)
# is provided by pymobiledevice3 11.2.x. Pin the exact version exercised by the
# kx.18 frozen-worker and package gates so a rebuild cannot silently change the
# per-UDID worker/fallback transport.
pymobiledevice3==11.2.4
fastapi>=0.141.1
uvicorn[standard]>=0.52.4
websockets>=16.0
gpxpy>=1.6.0
httpx>=0.27.0
pydantic>=2.13.5
python-multipart>=0.0.32
psutil>=6.0
